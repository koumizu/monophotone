<% provide(:search_name, 'ユーザー検索') %>
<% provide(:path, users_path) %>
<div class="users">
  <%= render "shared/search" %>
  <ul>
    <%= render @users %>
  </ul>
  <%= will_paginate @users, class: "paginate", :previous_label => ' &lt 前へ', :next_label => '次へ &gt' %>
</div>
